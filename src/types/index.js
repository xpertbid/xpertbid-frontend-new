// Core Types - JavaScript type definitions for development reference
// These are exported as empty objects to maintain compatibility with existing imports

// ContactFormData type structure
export const ContactFormData = {};

// BlogPost type structure  
export const BlogPost = {};

// Create a named object to avoid anonymous default export warning
const types = {
  ContactFormData,
  BlogPost
};

export default types;
